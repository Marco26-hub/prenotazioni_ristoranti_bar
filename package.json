{
  "name": "gestionale-ristoranti",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:guest": "pnpm --filter guest dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "bcryptjs": "^2.4.3",
    "postgres": "^3.4.5"
  }
}
